# Development-only dependencies (not needed at runtime inside QGIS).
-r requirements.txt

# Linter + formatter (configured in pyproject.toml)
ruff>=0.9.0

# Remote debugger — attach VS Code to the running QGIS process
debugpy>=1.8

# QGIS plugin packaging helper
pb-tool>=3.1
