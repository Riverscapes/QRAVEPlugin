# Runtime dependencies for the QRAVEPlugin QGIS plugin.
#
# QGIS itself provides: PyQt5/PyQt6, GDAL, sip, and the qgis.* namespace.
# Only list packages the plugin imports that are NOT bundled with QGIS.
#
# Install the local rsxml wheel separately:
#   pip install wheels/rsxml-2.2.1-py3-none-any.whl
lxml>=4.9,<6
requests>=2.28
